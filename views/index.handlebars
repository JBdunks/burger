<h1>Burgers</h1>

<h2>Burgers that have not been eatten</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{> burgers/burger-block devoured=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that have been eatten!</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block devoured=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Cook A New Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="burger">What kind of burger would you like?</label>
        <input type="text" id="burger" name="name">
    </div>


    <button type="submit">Burger</button>
</form>